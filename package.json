{
  "name": "gutenberg-rare-word-explorer",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "homepage": "https://rare-words.njmurray.com",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.34.0"
  }
}
